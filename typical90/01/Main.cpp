#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

#include "../lib/log.hpp"
#include "../lib/utility.hpp"

using namespace std;

int main() {
  int N, L;
  cin >> N >> L;
  int K;
  cin >> K;
  vector<int> A(N);
  for (auto& a : A)
    cin >> a;
  return 0;
}